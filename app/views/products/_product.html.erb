
<tr>
  <td><%= product.title %></td>
  <td><%= product.description %></td>
  <td><%= product.image_url %></td>
  <td><%= product.price %></td>
  <td>
    <!--<div class="mt-2"><%= link_to "Подробнее", product, class: "btn btn-primary" %></div>-->
    <div class="mt-2"><%= link_to "Редактировать", edit_product_path(product), class: "btn btn-secondary" %></div>
    <div class="mt-2"><%= button_to "Удалить", product, method: :delete, class: "btn btn-danger" %></div>
  </td>
</tr>