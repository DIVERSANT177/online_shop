<% content_for :title, "Editing order" %>

<h1><%= t('order.update') %></h1>

<%= render "form", order: @order %>

<br>

<div>
  <div class="text-center">
    <%= link_to t('btn.view_order'), @order, class: "btn btn-light mb-3" %>
  </div>
  <div class="text-center">
    <%= link_to t('btn.back'), orders_path, class: "btn btn-light" %>
  </div>
</div>
